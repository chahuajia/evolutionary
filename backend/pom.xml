<?xml version="1.0" encoding="UTF-8"?>
<!--
  第 1 轮只依赖 JUnit —— **不引 Spring Boot**。

  理由不是"省事"，是分层：领域层零框架依赖，
  于是它能**脱离框架被测试** —— 这是"分层是不是真的"的判据，不是口号。
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.evolutionary</groupId>
  <artifactId>evolutionary-backend</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <packaging>jar</packaging>

  <properties>
    <maven.compiler.release>17</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>5.10.2</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!--
        钉死 compiler plugin：Maven 3.8.1 默认用 3.1，而 3.1 **不支持**
        `maven.compiler.release` 属性 —— 属性被静默忽略，编译退化到 Java 8，
        于是 `instanceof` 模式匹配报"不支持"。
        （实测：不钉版本时 `mvn test` 直接编译失败。）
      -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.2.5</version>
      </plugin>
    </plugins>
  </build>
</project>
