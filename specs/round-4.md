# 第 4 轮：真实压测（非对照）

**日期**：2026-09-16 ｜ **性质**：用 evolutionary 压测 collaboration —— **不开 A/B**
**前置**：D 实验已停；症状表已加工程行；本轮要给 `known-gaps` 第一行一次**独立命中**机会

## 任务（业务）

在已有 `Battery` 之上实现：

1. **`Station`**：id、名称、一组在站电池；**至少 1 块 AVAILABLE 才能换出**
2. **`SwapSession`**：一次换电记录（哪站 / 换出哪块 / 换进哪块）
3. 领域层零框架；`mvn -B test` 全绿；**不 commit / 不 push**

## 暴露点（先声明，跑完不许改）

| # | 暴露点 | 怎么验收（可观察） |
| :-- | :--- | :--- |
| **P1** | 症状表工程行是否被**直接命中**（不靠 catalog 关键词碰运气） | 报告写清：是否读了 KB `AGENTS.md` 症状表；「不变量放哪一层」是否走到 `S13` / `parse-dont-validate` |
| **P2** | 「状态机怎么建模」是否按表走到 **known-gaps**（不发明专条） | 报告写清：有没有新建状态机条目；有没有记缺口 |
| **P3** | `design-decision` 三问是否在关键设计上**显式跑过** | 至少一个决策写出「没有它 / 收益 / 成本」 |
| **P4** | A10：规格是否先于实现 | 本文件落盘时间 ≤ 第一份 `.java` 新文件 |
| **P5** | 是否有真实**拦截**（条目拦住错误） | 有 → 记 `interceptions.md`；无 → 明确写 0，不编 |
| **P6** | E6 续：`S13` 的 `Result` 在 Java 侧 | 沿用异常还是引入 Result；理由是否来自库 |

## 本轮明确不做

- 不再跑对照臂 / 不改判据表
- 不批量填 `trigger`
- 不往 `agreements/` 加条（配额满）
- 不引入 Spring（第 1 轮分层判断延续，除非本轮 P3 推翻它）

## 交付

| 文件 | 内容 |
| :--- | :--- |
| `specs/round-4.md` | 本文件（暴露点） |
| `specs/round-4-report.md` | 逐条验收 + 检索路径 |
| `Station` / `SwapSession` + 测试 | 工作区未提交 |
